services:
  facerecserver:
    build:
      context: .
      args:
        BASE_IMAGE: python:3.12-slim
    image: facerecserver:cpu
    container_name: facerecserver
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./gallery:/app/gallery
    environment:
      - FACEREC_CONFIG=/app/facerecserver/config.yaml
      - TZ=Asia/Shanghai
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
